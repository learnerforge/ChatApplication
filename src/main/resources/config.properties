# ── Server ──────────────────────────────
server.port=5000
server.host=0.0.0.0
server.max_clients=100
server.backlog=50

# ── Database (SQLite by default) ────────
database.url=jdbc:sqlite:chatapp.db

# ── Chat history ────────────────────────
history.limit=50
